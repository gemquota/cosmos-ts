{
  "name": "cosmos-ts",
  "version": "0.1.0",
  "private": true,
  "description": "COSMOS — Comprehensive Ontological System for Meta-cognitive Orchestration & Synthesis",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "clean": "npm run clean --workspaces",
    "lint": "tsc --noEmit --workspaces"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.7.3"
  }
}
